{
  "name": "download-trap-guard",
  "version": "2.6.1",
  "private": true,
  "type": "module",
  "description": "Browser extension — warns when a Zoho Desk ticket has clickable images that trigger downloads",
  "scripts": {
    "build": "tsc && node scripts/build.mjs",
    "test": "npm run build && node tests/analysis.test.mjs",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.2"
  }
}
